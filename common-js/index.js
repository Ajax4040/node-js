//1) Forma común y clasica de importar un módulo llamada CommonJS
console.log('1) Forma común y clasica de importar un módulo llamada CommonJS');
const {sum} = require('./sum.js');

console.log(sum(1, 2));

